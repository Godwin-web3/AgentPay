# Secrets & env
.env
.env.local
.env.*.local
!.env.example
frontend/.env
frontend/.env.production
frontend/.env.local

# Runtime stores (contain user wallet ids, spend logs, schedules)
data/*.json
artifacts/*-deployment.json
config/agentDirectory.json

# Build
node_modules/
artifacts/deployment.json
package-lock.json
frontend/package-lock.json
*.bak

# Hardhat local test harness (test/*.test.js) — separate from the production
# artifacts/ dir written by scripts/compile.js + scripts/deploy.js.
hardhat-artifacts/
hardhat-cache/

# Misc
*.log
.DS_Store

# One-off exploration/scrape dumps (root only — src/data/*.json is real app data)
/active_providers_full.json
/all_minted_tokens.json
/agent-metadata.json
/confirmed_overlap_addresses.json
/registered_agents.json
/top_providers.json
/registered_provider_activity.json

# Applied one-off patch scripts (string-replace diffs from an earlier codegen pass)
/patch_*.py