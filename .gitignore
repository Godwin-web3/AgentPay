# Secrets & env
.env
.env.local
.env.*.local
!.env.example
frontend/.env
frontend/.env.production
frontend/.env.local

# Runtime stores (contain user wallet ids, spend logs, schedules)
data/*.json
artifacts/*-deployment.json
config/agentDirectory.json

# Build
node_modules/
artifacts/deployment.json
package-lock.json
frontend/package-lock.json
*.bak

# Hardhat local test harness (test/*.test.js). Separate from production artifacts/.
hardhat-artifacts/
hardhat-cache/

# Scraped / generated dumps that do not belong in a clone
all_minted_tokens.json
active_providers_full.json
confirmed_overlap_addresses.json
patch_*.py

# Misc
*.log
.DS_Store
