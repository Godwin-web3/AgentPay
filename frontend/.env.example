VITE_WORKER_URL=http://localhost:3000
VITE_RPC_URL=https://rpc.testnet.arc.network
# Must match the backend APP_API_KEY. For the demo this is the shared operator
# key; per-user keys would be issued out-of-band and stored per-user instead.
VITE_APP_API_KEY=
